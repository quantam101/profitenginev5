# No-Spend Policy

The source of truth is `security/no-spend-policy.yaml` and `eaos.config.yaml`.

This document intentionally does not duplicate policy values. Update the YAML and run `npm run validate:yaml`.
