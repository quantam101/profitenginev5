module:
  id: tradegate
  name: tradegate
  version: 0.1.0
  state: disabled_stale_source
  cost_mode: zero_spend
  enabled: false
  disabled_reason: "Disabled stale source is not production Trade Gate source. Keep disabled until replaced by verified backend/UI deployment."
  requires_approval_for:
    - external_write
    - paid_call
    - production_change
  healthcheck: null
